{
  "webmonitor": {
    "basePath": "{{ PUBLIC_PATH }}/monitor/",
    "port": 36001
  },
  "webadmin": {
    "protocol": "{{ PUBLIC_PROTOCOL }}",
    "host": "{{ PUBLIC_HOSTNAME }}",
    "port": {{ PUBLIC_PORT }},
    "basePath": "{{ PUBLIC_PATH }}/admin/"
  }
}
